note: see schalbal_build.sh, only the scelbal.mac is used.  The other scelbal8080 and .asm files are for 8008 processor earlier versions.

You have been using test jig src/trace_scelbal.cc to run scelbal in an emulator you can trap and get debug info out of.

 Completed:
  - RUN command works - programs execute correctly
  - Fixed multiple bugs in NOTEND, INSERT, buffer allocation, and overflow detection

  In Progress - FA Stack Issues:
  The FA (Function/Array) stack has addressing problems. Key findings:
  - FA_STKPTR (0x177B) and FA_STACK (0x186A) are NOT adjacent in memory
  - Original 8008 code assumed page-based addressing where both were on same page
  - Current fixes changed PRIGHT, FUNAR4, and SCAN6 to use FA_STACK-1 as base instead of FA_STKPTR
  address

  Current Symptoms:
  - PRINT 5 → works ("5.0")
  - PRINT (5) → "5.0FA" (value correct, then FA error)
  - PRINT((5)) → "FA" (pure error, no value)
  - PRINT INT(5) → "5.0FA" (function works but FA error follows)

  Next Steps to Debug:
  1. Trace why FA error occurs after successful evaluation
  2. Check if FUNARR is being called when it shouldn't (with non-empty SYMBOL_BUF)
  3. Investigate the FUNAR2 loop which searches arrays - may infinite loop when NUM_DIM_ARRAYS=0
  4. Consider if other code paths are using the old FA_STKPTR+offset addressing pattern

